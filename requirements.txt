# Web server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# HTTP client (metadata scraping)
httpx>=0.27.0

# Image processing — covers + WebP conversion
Pillow>=10.3.0

# PDF cover extraction (optional — remove if not needed)
pymupdf>=1.24.0
