# BookLibrary — Python dependencies
# Install with: pip install -r requirements.txt

# ── Core ─────────────────────────────────────────────────────────────────────
fastapi>=0.110.0        # Web framework
uvicorn[standard]>=0.27 # ASGI server (includes websockets, httptools)
pydantic>=2.0           # Data validation and serialization
httpx>=0.27             # Async HTTP client (metadata scraping)

# ── Optional ─────────────────────────────────────────────────────────────────
# Uncomment to enable the corresponding features:

# Pillow>=10.0            # WebP image conversion (required for --webp flag)
# python-dotenv>=1.0      # Richer .env syntax (multiline, comments, quoted values)
#                         # Falls back to built-in parser if not installed
# pymupdf>=1.24           # PDF cover extraction (fitz)
